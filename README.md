# Budget-app
A financial management application built with Java Spring Boot (backend) and React + Typescript (frontend). Data is stored in a database running in a Docker container. Users can register, add, edit, and delete their expenses, as well as share them with others
